import { defineStore } from 'pinia'

export const useUserStore = defineStore('user', {
  state: () => ({
    Users: [],
  }),
  getters: {
    getUserName: state => {
      let User = 0
      for (let p in phoneNumbers) {
        console.log()
        User = cartItem.quantity * cartItem.price
      }
      return total
    },
    }
})
